# ─────────────────────────────────── Go ────────────────────────────────
# Бинарники
*.exe
*.exe~
*.dll
*.so
*.dylib
/backend/api
/backend/seeder

# Тестовые бинарники и артефакты покрытия
*.test
*.out
coverage.*
*.coverprofile
profile.cov

# Каталог зависимостей (раскомментируйте, если решите вендорить)
# vendor/

# Go workspace
go.work
go.work.sum

# ──────────────────────────────── Node ─────────────────────────────────
node_modules/
dist/
build/
.vite/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Типы, сгенерированные из OpenAPI: генерируются командой make api-types.
# Правило снимается, если команда решит коммитить их для стабильности сборки.
/frontend/src/api/schema.ts

# ────────────────────────────── Окружение ──────────────────────────────
# Секреты в репозиторий не попадают (SEC5). Пример — в .env.example.
.env
.env.*
!.env.example

# ─────────────────────────── Редакторы и ОС ────────────────────────────
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
